---
name: 同步测试版到生产版
description: 测试版验证通过后需要同步到生产版的所有文件清单
type: project
---

## 同步清单

测试版 `plant-encyclopedia-d-d1af4e84f48` → 生产版 `plant-encyclopedia-8d9x10139590b`

### 云函数（4个）

| 云函数 | 修改内容 | 数据库集合 |
|--------|----------|------------|
| `analytics_track` | 添加 `openId` 参数支持，解决云函数之间调用时无法获取 openId 的问题 | analytics_daily, analytics_events, analytics_users |
| `identifyPlant` | 调用 `analytics_track` 时传递 `openId`；修复变量名 `openId` → `userOpenId` | - |
| `getCareGuide` | 精简 Prompt 模板；减少 max_tokens；添加重试机制；字段映射 `description` | - |
| `getAnalytics` | **新增** - 数据看板统计云函数，读取 analytics_daily/users/events 数据 | analytics_daily, analytics_events, analytics_users |

### 不需要同步的云函数

| 云函数 | 原因 |
|--------|------|
| `resetAnalytics` | 仅用于清空测试数据，生产环境不需要（清空真实用户数据是危险操作） |

### 管理控制台调用的云函数（3个）

| 云函数 | 用途 | 备注 |
|--------|------|------|
| `getAppConfig` | 校验管理员身份、加载配置 | 已存在 |
| `getAnalytics` | 获取数据看板统计数据 | **新增** |
| `updateAppConfig` | 更新功能开关配置 | 已存在 |

### 不需要同步

| 云函数 | 原因 |
|--------|------|
| `resetAnalytics` | 仅用于清空测试数据，生产环境不需要（清空真实用户数据是危险操作） |

### 前端文件（2个）- 上传发布时自动同步

| 文件路径 | 用途 | 备注 |
|----------|------|------|
| `package-plant/pages/search_result/search_result.js` | 搜索结果页 fallback 数据处理 | 本地代码，上传发布即可 |
| `config/video-config.js` | 首页视频路径配置 | **需先修改为生产环境视频路径** |

### 管理控制台（3个）

| 文件路径 | 修改内容 |
|----------|----------|
| `package-admin/pages/admin/admin.js` | 管理控制台入口 |
| `package-admin/pages/admin/admin.wxml` | 管理控制台入口页面 |
| `package-admin/pages/admin/admin.wxss` | 管理控制台入口样式 |
| `package-admin/pages/admin/dashboard.js` | 数据看板逻辑 |
| `package-admin/pages/admin/dashboard.wxml` | 数据看板页面 |
| `package-admin/pages/admin/dashboard.wxss` | 数据看板样式 |

---

## 同步步骤

1. 上传云函数到生产环境（4个）
2. 前端代码已在本地，上传发布即可
3. 管理控制台代码已在本地，上传发布即可

## 注意事项

- 前端代码修改不需要微信审核
- 直接上传发布即可
